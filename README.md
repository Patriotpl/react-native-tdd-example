# react-native-tdd-example
``` sh
npx react-native init tddExample --template react-native-template-typescript
yarn add @testing-library/react-native react-native-linear-gradient
npx pod-install ios
```
